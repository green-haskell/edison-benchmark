module Types where

import qualified Data.Text.Lazy as DTL (
    Text ( .. )
    )


-- ---------- ---------- ---------- ---------- ---------- ---------- ----------
type SequenceId = DTL.Text

type BenchmarkId = DTL.Text
type BenchmarkOutput = DTL.Text

